rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Read-only public access — writes only happen via Admin SDK in scripts
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
